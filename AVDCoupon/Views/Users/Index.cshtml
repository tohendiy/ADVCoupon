@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>


@Html.AjaxGrid(Url.Action("IndexGrid"))

@*<table class="table">
        <thead>
            <tr>
                <th>
                    Email
                </th>
                <th>
                    Role
                </th>
                <th>
                    Provider
                </th>
                <th>
                    Network
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.User.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Role)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.Provider.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.Network.Caption)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.User.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.User.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.User.Id">Delete</a>
                </td>
            </tr>
    }
        </tbody>
    </table>*@
